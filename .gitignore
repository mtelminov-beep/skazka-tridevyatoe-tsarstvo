node_modules/
dist/
*.log
*.tsbuildinfo
frontend/dist-desktop/
frontend/public/media/uploads/
release/
activation-tool/TridevyatoeActivationTool/keys/*.pfx

# Блокировка журнала версий — временный файл конкретного процесса.
# Без этой строки `bump` коммитит собственный lock и оставляет рабочее
# дерево грязным сразу после того, как записал версию.
.versions/.lock

# Базовый каталог CMS и комплект иллюстраций синхронизируются вместе с проектом,
# чтобы новая локальная копия сразу показывала обложки и материалы разделов.
